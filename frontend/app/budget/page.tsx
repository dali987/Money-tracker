import { StrikethroughIcon } from 'lucide-react';
import React from 'react';

const page = () => {
    return (
        <main className="bg-white lg:bg-gray-200 flex justify-center items-start lg:items-center min-h-screen w-full lg:w-[calc(100%-var(--nav-width))] lg:ml-(--nav-width) p-4 lg:p-16">
            <section className="w-full lg:bg-white lg:rounded-lg lg:shadow-2xl lg:max-w-5xl p-4 flex flex-col gap-4">
                <h1>Its not done yet in the original project :)</h1>
            </section>
        </main>
    );
};

export default page;
